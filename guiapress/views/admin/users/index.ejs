<%- include('../../partials/head.ejs') %>
    <%- include('../../partials/navbar.ejs') %>
        <div class="container">
            <hr>
            <h2>Usuarios</h2>
            <hr>
            <table class="table table-bordered  table-hover">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Email</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(users=> { %>
                        <tr>
                            <th>
                                <%= users.id %>
                            </th>
                            <th>
                                <%= users.email %>
                            </th>
                            <th>
                                <a href="/admin/users/edit/<%=users.id %>" class="btn btn-warning" style="width:45%"><i style="color:rgb(247, 247, 247)" class="fas fa-cog"></i></a>
                                <form action="/users/delete" method="post" style="display:inline"
                                    onsubmit="confirmarDelecao(event,this)">
                                    <input type="hidden" name="id" value="<%= users.id %>">
                                    <button class="float-right btn btn-danger" style="width:45%"><i class="fa fa-times"></i></button>
                                </form>
                            </th>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
            <a href="/admin/users/create" class="btn btn-success float-right">Cadastrar Usuario</a>
        </div>
        <script>
            function confirmarDelecao(event,form) {
                event.preventDefault();
                var decision = confirm("Você quer deletar essa categoria")
                if (decision) {
                    form.submit();
                }
            }
        </script>
        <%- include('../../partials/footer.ejs') %>