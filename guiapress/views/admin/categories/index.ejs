<%- include('../../partials/head.ejs') %>
    <%- include('../../partials/navbar.ejs') %>
        <div class="container">
            <hr>
            <h2>Categorias</h2>
            <hr>
            <table class="table table-bordered  table-hover">
                <thead>
                    <tr>
                        <th>Categoria</th>
                        <th>Slug</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
                    <% categories.forEach(category=> { %>
                        <tr>
                            <th>
                                <%= category.title %>
                            </th>
                            <th>
                                <%= category.slug %>
                            </th>
                            <th>
                                <a href="/admin/categories/edit/<%=category.id %>" class="btn btn-warning" style="width:45%"><i style="color:rgb(247, 247, 247)" class="fas fa-cog"></i></a>
                                <form action="/categories/delete" method="post" style="display:inline"
                                    onsubmit="confirmarDelecao(event,this)">
                                    <input type="hidden" name="id" value="<%= category.id %>">
                                    <button class="float-right btn btn-danger" style="width:45%"><i class="fa fa-times"></i></button>
                                </form>
                            </th>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
            <a href="/admin/categories/new" class="btn btn-success float-right">Cadastrar categoria</a>
        </div>
        <script>
            function confirmarDelecao(event,form) {
                event.preventDefault();
                var decision = confirm("Você quer deletar essa categoria")
                if (decision) {
                    form.submit();
                }
            }
        </script>
        <%- include('../../partials/footer.ejs') %>